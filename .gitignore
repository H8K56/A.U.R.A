# Build artifacts (in Docker volumes, but just in case)
build/
install/
log/
node_modules/

# Files rescued from the container's ephemeral layer during the
# single-clone migration; keep locally, not in the repo.
_container_backup/

# Local Claude Code settings
.claude/settings.local.json

# Simulation recordings — large, kept on disk but out of the repo.
# The demo video is excepted: README.md links to it directly.
*.mp4
!aura_test_simflight.mp4

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Trained models (large files)
models/*.pt
models/*.pth
models/*.onnx

# Logs
*.log
wandb/
tensorboard/

# Temporary files
*.tmp
*.bak
