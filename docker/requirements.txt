# A.U.R.A. — direct Python dependencies, pinned to the known-good container.
#
# Versions are what the working aura-dev container actually runs, captured
# 2026-09-14 — not aspirational pins. The previous Dockerfile asked for
# numpy==1.24.3 and got 1.26.4, because a later unpinned install overrode it;
# pinning the whole set together is what stops that.
#
# torch is NOT listed here: it needs --index-url for the CUDA variant, so the
# Dockerfile installs it separately from TORCH_* in versions.env.
#
# Full environment record: requirements.lock.txt
# Non-pip components (PX4, px4_msgs, ns-3, ...): versions.env

# Build backend. pip freeze omits setuptools by default, so these were missing
# from the first cut of this file — and CI promptly broke on it: a modern
# setuptools against Jammy's apt packaging (21.3) raises
# "canonicalize_version() got an unexpected keyword argument
# 'strip_trailing_zero'" while building the ament Python eggs. Pin both.
setuptools==80.10.2
packaging==26.0

# Scientific stack
numpy==1.26.4
scipy==1.11.4
pandas==2.0.3
matplotlib==3.7.5
seaborn==0.13.0
scikit-learn==1.3.2

# sympy/mpmath are installed over the apt versions, which the PX4 build needs
sympy==1.13.1
mpmath==1.3.0

# RL stack. train_policy implements PPO directly on torch + gymnasium; the
# rest of the baseline image's RL packages are in
# requirements-baseline-extra.txt because nothing imports them.
gymnasium==0.29.1

# Tooling
pyyaml==6.0.1
rich==13.7.0
typer==0.9.0
pytest==7.4.3
black==23.12.1
ruff==0.1.8
